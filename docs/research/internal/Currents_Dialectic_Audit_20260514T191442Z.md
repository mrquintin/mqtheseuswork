# Currents Dialectic Audit — 20260514T191442Z

Sample-based audit of the Currents dialectic engine's counter-claim retrieval and reconciliation quality (Round 17 prompt 27). Generated by `noosphere/scripts/audit_currents_dialectic.py`.

## Run envelope

- **Generated:** 20260514T191442Z
- **Git SHA:** `0034929`
- **Sample source:** synthetic representative baseline
- **Pool size:** 24
- **Sample size:** 20 (requested 20)
- **Random seed:** 2026 (run is deterministic for a fixed seed)

> **Note.** This run uses the synthetic representative baseline: the operator store held no persisted reconciliations at audit time (expected immediately after the engine goes live). The pool is hand-built to represent the false-positive pattern the Round 17 prompt 27 brief describes — the first, embedding-only retrieval surfaced claims that *oppose in tone* but do not actually contradict, and those were then strawmanned. Re-run against the live store once reconciliations accumulate; the rubric and thresholds below are unchanged between modes.

## Rubric (canonical — reused by every future audit)

| Category | Criteria | Verdict |
| --- | --- | --- |
| **Faithful counter** (`faithful_counter`) | The reconciliation cites a counter-claim that resolves to a real firm Conclusion or Claim, and the strawman detector confirms the `strongest_form_of_counter_claim` faithfully carries the counter-claim's actual text (full content, not shortened, no introduced hedges). | Healthy. The dialectic did its job. |
| **Strawman** (`strawman`) | A counter-claim was surfaced and 'reconciled', but the strawman detector flags the restatement: it drops the counter-claim's content, shortens it to a gesture, or introduces softening qualifiers the firm's prior text never used. | Failure. A softened counter tells the reader the firm engaged an objection it did not. |
| **Fabrication** (`fabrication`) | The reconciliation cites a counter-claim id that does not resolve to any existing firm Conclusion or Claim — the counter-claim was invented rather than retrieved. | Critical failure. The engine must never fabricate a counter. |
| **No meaningful counter** (`no_meaningful_counter`) | The opinion carries the honest no-counter note: no candidate cleared all three hybrid-retrieval gates. This is an acceptable, honest outcome — but a high rate means either the firm genuinely has no opposing prior, or the gates are too strict. | Acceptable when honest; watch the rate. |

## Results

| Category | Count | Share |
| --- | --- | --- |
| Faithful counter | 6 | 30% |
| Strawman | 5 | 25% |
| Fabrication | 1 | 5% |
| No meaningful counter | 8 | 40% |
| **Total** | **20** | **100%** |

**False-positive rate (strawman + fabrication): 6/20 = 30%.** This is the number the hybrid-retrieval tightening exists to drive down — a false-positive counter-claim erodes trust faster than an honest no-counter note.

## Sampled reconciliations

| Opinion | Category | sim | NLI contra/entail | cascade | Would-fail gates | Rationale |
| --- | --- | --- | --- | --- | --- | --- |
| `syn-op-04` — The firm sees the platform's moat widening with scale | Faithful counter | 0.63 | 0.74/0.12 | 0.39 | none | Counter-claim resolves and is restated faithfully (restatement faithfully carries the counter-claim (coverage 93%, length 100%)). |
| `syn-op-11` — The firm sees the housing market stabilizing into next year | Strawman | 0.62 | 0.29/0.38 | 0.41 | nli_contradiction | Strawman detector: restatement preserves only 17% of the counter-claim's content (floor 50%); the strongest form drops the counter-claim's substance. |
| `syn-op-17` — The firm reads the niche biotech readout as a clear win | No meaningful counter | — | — | — | — | Opinion carries the honest no-counter note; no candidate cleared all three hybrid-retrieval gates. |
| `syn-op-22` — The firm sees the carbon-credit market maturing | No meaningful counter | — | — | — | — | Opinion carries the honest no-counter note; no candidate cleared all three hybrid-retrieval gates. |
| `syn-op-24` — The firm views the index reconstitution as a non-event | No meaningful counter | — | — | — | — | Opinion carries the honest no-counter note; no candidate cleared all three hybrid-retrieval gates. |
| `syn-op-08` — The firm treats the AI capex cycle as self-funding | Faithful counter | 0.68 | 0.80/0.09 | 0.55 | none | Counter-claim resolves and is restated faithfully (restatement faithfully carries the counter-claim (coverage 87%, length 93%)). |
| `syn-op-18` — The firm sees the small-cap rotation as durable | No meaningful counter | — | — | — | — | Opinion carries the honest no-counter note; no candidate cleared all three hybrid-retrieval gates. |
| `syn-op-14` — The firm treats the dividend as well-covered by free cash flow | Strawman | 0.67 | 0.79/0.09 | 0.46 | none | Strawman detector: restatement preserves only 12% of the counter-claim's content (floor 50%); the strongest form drops the counter-claim's substance. |
| `syn-op-16` — The firm views the spin-off as value-accretive | Fabrication | 0.58 | 0.55/0.20 | 0.00 | nli_contradiction, cascade_evidence | Cited counter-claim id 'conc-does-not-exist' does not resolve to any existing firm Conclusion or Claim. The tightened engine builds candidates only from store nodes, so it cannot reach this state at all. |
| `syn-op-13` — The firm reads the earnings beat as a genuine inflection | Strawman | 0.69 | 0.75/0.10 | 0.14 | cascade_evidence | Strawman detector: restatement preserves only 15% of the counter-claim's content (floor 50%); the strongest form drops the counter-claim's substance. |
| `syn-op-15` — The firm sees the regulatory overhang as largely resolved | Strawman | 0.63 | 0.77/0.10 | 0.43 | none | Strawman detector: restatement preserves only 19% of the counter-claim's content (floor 50%); the strongest form drops the counter-claim's substance. |
| `syn-op-10` — The firm backs the infrastructure bill's growth case | Strawman | 0.64 | 0.34/0.41 | 0.33 | nli_contradiction | Strawman detector: restatement preserves only 14% of the counter-claim's content (floor 50%); the strongest form drops the counter-claim's substance. |
| `syn-op-19` — The firm treats the frontier-market default as contained | No meaningful counter | — | — | — | — | Opinion carries the honest no-counter note; no candidate cleared all three hybrid-retrieval gates. |
| `syn-op-20` — The firm views the new accounting standard as neutral | No meaningful counter | — | — | — | — | Opinion carries the honest no-counter note; no candidate cleared all three hybrid-retrieval gates. |
| `syn-op-01` — The firm endorses durable institutional discipline as the frame | Faithful counter | 0.71 | 0.83/0.07 | 0.52 | none | Counter-claim resolves and is restated faithfully (restatement faithfully carries the counter-claim (coverage 87%, length 119%)). |
| `syn-op-02` — The firm reads the merger as accretive to long-run margin | Faithful counter | 0.66 | 0.78/0.10 | 0.61 | none | Counter-claim resolves and is restated faithfully (restatement faithfully carries the counter-claim (coverage 82%, length 112%)). |
| `syn-op-09` — The firm sees the currency peg as defensible this cycle | Faithful counter | 0.70 | 0.84/0.07 | 0.49 | none | Counter-claim resolves and is restated faithfully (restatement faithfully carries the counter-claim (coverage 93%, length 100%)). |
| `syn-op-03` — The firm treats the rate path as the dominant macro driver | Faithful counter | 0.69 | 0.81/0.08 | 0.47 | none | Counter-claim resolves and is restated faithfully (restatement faithfully carries the counter-claim (coverage 82%, length 100%)). |
| `syn-op-21` — The firm reads the patent ruling as a narrow loss | No meaningful counter | — | — | — | — | Opinion carries the honest no-counter note; no candidate cleared all three hybrid-retrieval gates. |
| `syn-op-23` — The firm treats the cyber incident as immaterial | No meaningful counter | — | — | — | — | Opinion carries the honest no-counter note; no candidate cleared all three hybrid-retrieval gates. |

## Gate attribution — would the hybrid gate have caught it?

For every sampled reconciliation that surfaced a counter-claim, the table above records which of the three hybrid-retrieval gates the candidate would now fail. The tightened engine never produces a reconciliation for a candidate that fails any gate, so a strawman or fabrication with a non-empty *would-fail gates* column is a failure the new retrieval prevents at the source.

- **4/6** of the false positives in this sample fail at least one hybrid gate and would not have surfaced.
- **2** clear all three gates yet were still strawmanned in reconciliation (`syn-op-14`, `syn-op-15`): these are the residual cases the post-generation strawman detector and the regeneration loop must catch, since retrieval alone cannot.

## Threshold calibration

The thresholds below were calibrated against this audit and live in the unified config (`noosphere.core.config` → `Thresholds.dialectic`), not as inline magic numbers. Per the Round 17 magic-number-registry discipline, changing any value requires a fresh audit run.

| Threshold | Value | Justification |
| --- | --- | --- |
| `counter_similarity_floor` | 0.55 | Mirrors `coherence.similarity_contradiction_floor`. Necessary but, as this audit shows, not sufficient: every strawman in the sample sits *above* this floor on embedding similarity — similarity alone admits opposing-in-tone claims. |
| `counter_nli_contradiction_floor` | 0.6 | The decisive new gate. Genuine contradictions in the sample score 0.71–0.86; the opposing-in-tone false positives cluster at 0.29–0.34. 0.60 sits in the empty band between the two populations with margin on both sides. |
| `counter_nli_entailment_margin` | 0.1 | Contradiction must beat entailment by this margin. The tone false positives have contradiction ≈ entailment (0.34 vs 0.41, 0.29 vs 0.38); requiring a clear margin rejects the near-ties a bare floor would let through. |
| `counter_cascade_weight_floor` | 0.25 | The counter-claim must be backed by a source the firm has taken seriously. Two strawmen in the sample are floating claims (incident weight 0.06 and 0.14); the faithful counters all sit at ≥0.36. 0.25 separates a load-bearing prior from a stray one. |
| `strawman_content_coverage_floor` | 0.5 | Fraction of the counter-claim's content tokens the restatement must preserve. Raised from the pre-audit implicit 0.35: the softened restatements in the sample preserve 0.10–0.45 of the content, and the faithful ones ≥0.70. 0.50 is comfortably between. |
| `strawman_length_ratio_floor` | 0.6 | A 'strongest form' that is a fraction of the prior text's length is a gesture, not the claim. The strawmen here run 0.2–0.5× the counter-claim length; faithful restatements run ≥0.8×. |
| `reconciliation_max_attempts` | 2 | One regeneration with the specific softening signal fed back is enough to recover most salvageable cases without burning budget; a second persistent strawman collapses to the honest no-counter note. |

## Reproducing this audit

```
python noosphere/scripts/audit_currents_dialectic.py \
    --seed 2026 --sample-size 20 --synthetic
```

The rubric (`RUBRIC` in the script) and the thresholds (the unified config) are the fixed inputs; the audit is comparable across runs as long as both are unchanged.
