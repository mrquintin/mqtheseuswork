# Severity Calibration — Status

_Generated by `noosphere/scripts/fit_severity_model.sh` — do not edit by hand. Regenerated on every nightly run._

- **Generated at:** 2026-05-14T23:52:17.996786+00:00
- **Status:** `cold_start`
- **Labeled objections:** 0 (threshold to fit: 50)
- **Outcome mix:** 0 material-change · 0 addendum · 0 dismissed
- **Corpus:** `noosphere_data/severity_calibration/labeled_objections.jsonl`

## Deliberate deferral

only 0 labeled objection(s) — below the cold-start threshold of 50. Fitting a logistic model on data this thin would ship a noisy scorer; the stipulated rubric is the honest fallback until the corpus grows.

The stipulated severity rubric (`noosphere.peer_review.severity.score_objection`) remains the active scorer. This is a deliberate choice, not a gap: fitting a logistic model on a corpus this thin would ship a noisy scorer dressed up as a measurement. The firm would rather run the honest stipulated formula and say so than swap in a model it cannot defend.

The replacement is gated on sample size: it engages automatically once the labeled corpus reaches **50** objections with both outcome classes present. Until then, no action is required — the nightly job re-checks the corpus and this note is its output.

