You are working in `/Users/michaelquintin/Desktop/Theseus`.

Goal: build a founder-alpha setup surface for prediction-market portfolios and exchange credentials.

Product direction:
The founder should have a clear setup flow in Theseus Codex showing exactly what must be configured for Polymarket and Kalshi. Once the required values are present, the system should verify connectivity, show readiness, and begin monitoring/decision generation. Live capital deployment still requires explicit live enablement, bankroll/risk limits, authorization, and kill-switch health.

Start by reading:
- `docs/architecture/Algorithmized_Decision_Making.md`
- `noosphere/noosphere/forecasts/config.py`
- `noosphere/noosphere/forecasts/safety.py`
- `noosphere/noosphere/forecasts/_polymarket_live_client.py`
- `noosphere/noosphere/forecasts/_kalshi_live_client.py`
- `theseus-codex/src/app/(authed)/forecasts/operator/page.tsx`
- `theseus-codex/src/app/(authed)/forecasts/portfolio/page.tsx`
- forecast operator API routes;
- existing env/secret docs.

Implement:
1. Add a founder-alpha setup page or panel, preferably under the authenticated forecasts/operator area:
   - exchange connection status;
   - required environment variable names;
   - whether each required secret is configured on the server;
   - read-only connectivity test results;
   - paper/live mode readiness;
   - bankroll and risk-limit checklist;
   - kill-switch state.
2. Do not store private exchange keys raw in the application database.
   - If the repo already has an encrypted secrets mechanism, use it.
   - If it does not, provide a server-environment setup flow and instructions instead of a raw key form.
   - If you add a credential ingestion endpoint, it must use envelope encryption or a managed secret store; never persist raw keys.
3. Add API route(s) for read-only setup status:
   - Polymarket configured yes/no;
   - Kalshi configured yes/no;
   - live flag enabled yes/no;
   - max stake/max daily loss configured;
   - scheduler status file present/fresh;
   - last read-only exchange connectivity check.
4. Add explicit instructions for the founder:
   - what keys are needed;
   - where to place them in local development;
   - where to place them in deployment;
   - what not to paste into public/support channels;
   - how to verify readiness.
5. Make the UX honest:
   - "Monitoring active" only when scheduler status is fresh;
   - "Ready for live candidates" only when data/risk/credential gates pass;
   - "Ready for live orders" only when all safety prerequisites pass.

Constraints:
- Do not collect or display full private keys in client-rendered UI.
- Do not log private keys.
- Do not auto-enable live trading just because credentials exist.
- Preserve founder permissions.

Verification:
- Run `npm run build` in `theseus-codex`.
- Run focused forecast operator tests if present.
- Browser-check forecasts operator/setup pages with missing credentials and confirm instructions are clear.

SCOPE:
MODIFY theseus-codex/src/app/(authed)/forecasts/operator/page.tsx
MAYBE ADD theseus-codex/src/app/(authed)/forecasts/setup/page.tsx
MAYBE ADD theseus-codex/src/app/(authed)/api/forecasts/operator/setup-status/route.ts
MAYBE MODIFY theseus-codex/src/lib/forecastsOperatorApi.ts
MAYBE MODIFY noosphere/noosphere/forecasts/config.py only for missing status helpers.
MAYBE ADD docs/operations/Forecasts_Portfolio_Setup.md

