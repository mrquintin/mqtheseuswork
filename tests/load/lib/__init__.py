"""Load-test runner library.

Public surface lives in ``runner``. The package is intentionally
zero-dependency (stdlib only) so the harness can run in a stripped-down
CI image without a Python venv install step.
"""
