# Claude Code Prompt 17 of 22 — Homepage teaser, nav pulse, transparency footer on /currents

**Sequence:** prompt 17 of 22 (Wave 6 opener). Depends on 12, 13, 16. Report back with: teaser failure-mode behavior, footer copy, nav placement.

**Why this prompt exists.** The currents feature is now functional under `/currents`, but a visitor landing on the homepage has no way to find it. We need three small but important integrations: (1) a teaser on the homepage showing the latest 3 opinions, (2) the `<CurrentsNavPulse>` link in the root nav, (3) a transparency footer on every `/currents*` page disclaiming the LLM origin and the abstention policy.

**Before you start — read current state:**
1. Read `theseus-codex/src/app/page.tsx` post-merger — the public blog index.
2. Read `theseus-codex/src/app/layout.tsx` — root nav.
3. Read `theseus-codex/src/app/currents/layout.tsx` (prompt 12) — where the footer lives.

## Your task

### 1. Homepage teaser

In `theseus-codex/src/app/page.tsx`, add a server component `<CurrentsTeaser />` ABOVE the existing posts list:

```tsx
async function CurrentsTeaser() {
  let items: PublicOpinion[] = [];
  try {
    const resp = await listCurrents({ limit: 3 });
    items = resp.items;
  } catch { return null; }              // graceful: API down → show nothing
  if (!items.length) return null;        // graceful: no opinions yet → show nothing

  return (
    <section style={{ /* parchment-bordered card */ }}>
      <div>{/* "Live · current events" eyebrow + "See all →" link */}</div>
      <ul>{items.map(op => <li key={op.id}>{/* compact row */}</li>)}</ul>
    </section>
  );
}

export default async function PublicBlogIndex() {
  const founder = await getFounder();
  const posts = await db.upload.findMany({/* existing */});
  return (
    <main className="container">
      <CurrentsTeaser />
      {/* existing posts list */}
    </main>
  );
}
```

The teaser MUST fail gracefully — a backend outage cannot break the homepage. The component returns `null` on any error.

### 2. Nav pulse

In `theseus-codex/src/app/layout.tsx`, add `<CurrentsNavPulse />` (from prompt 12) into the existing nav, between "Index" and "Methodology" (or whatever the appropriate slot is post-merger). The pulse is purely client-side CSS — it works regardless of backend state.

### 3. Transparency footer

In `theseus-codex/src/app/currents/layout.tsx`, append after `{children}`:

```tsx
<footer style={{ marginTop: "3rem", paddingTop: "1.1rem",
                 borderTop: "1px solid var(--currents-border)",
                 fontSize: "0.78rem", lineHeight: 1.6,
                 color: "var(--currents-parchment-dim)", fontStyle: "italic" }}>
  <p style={{ margin: 0, maxWidth: "52em" }}>
    Opinions on this page are generated by Claude Haiku 4.5 from the firm's
    published Noosphere of conclusions and claims. They are not editorial
    commentary, not legal or financial advice, and not an exhaustive survey
    of the firm's thinking. The firm abstains from opining when its sources
    do not support a position; an abstention is not agreement with the
    observed event. Citations link to verbatim source passages; when a
    source is revoked, dependent opinions are marked accordingly. Follow-up
    answers re-retrieve from the Noosphere on each question — the assistant
    does not answer from memory.
  </p>
</footer>
```

This copy is load-bearing — every founder + every reader needs to know what the page is and isn't. Don't paraphrase without checking with the operator.

## Tests

`CurrentsTeaser.test.tsx`:
- Mocked `listCurrents` returning 3 → renders 3 rows + "See all" link.
- Mocked rejection → returns null (no homepage breakage).
- Empty array → returns null.

## Prerequisites

- Prompts 12, 13, 16 applied.

## SCOPE

- `theseus-codex/src/app/page.tsx`                                         MODIFY (add CurrentsTeaser above existing content)
- `theseus-codex/src/app/layout.tsx`                                       MODIFY (add CurrentsNavPulse to nav)
- `theseus-codex/src/app/currents/layout.tsx`                              MODIFY (append transparency footer)
- `theseus-codex/src/__tests__/CurrentsTeaser.test.tsx`                    CREATE

## Verification

```bash
cd /Users/michaelquintin/Desktop/Theseus/theseus-codex
npm test
npm run build
# Manual: with FastAPI down, homepage still renders the existing posts list cleanly.
# With FastAPI up + opinions seeded, teaser renders.
```

## Prohibitions

- Do NOT throw from `<CurrentsTeaser>`. Return null on any error.
- Do NOT paraphrase the transparency footer copy. The exact wording is the disclosure.
- Do NOT add tracking pixels, UTM params, or analytics anywhere on `/currents*`.
- Do NOT remove existing nav items.

Do not proceed to prompts 18+. Do not edit anything outside SCOPE.
